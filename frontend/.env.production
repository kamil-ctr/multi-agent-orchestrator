# Replaced with the real Render backend URL during Vercel setup
# (either edit this file before building, or set VITE_API_URL as a
# Vercel project environment variable — the latter takes precedence
# for Vercel-triggered builds and doesn't require a commit).
VITE_API_URL=https://REPLACE_WITH_RENDER_URL.onrender.com
